@model IEnumerable<OutXApp.Core.Models.Course>
    @{
        Layout = "~/Views/Shared/_Layout.cshtml";
    }

<div class="page-title">
    <div class="title_left">
        <h3>Courses</h3>
    </div>
</div>

<div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_content">

                <table id="Courses" class="data table table-striped no-margin">
                    <thead>
                        <tr>
                            <th>Token</th>
                            <th>Title</th>
                            <th>Phone</th>
                            <th>Address</th>
                            <th>City</th>
                            <th><a href="@Url.Action("Create","Courses")" class="btn btn-primary btn-xs"><i class="fa fa-edit"></i>Add</a></th>

                        </tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>
    </div>
</div>
@section scripts
{

    <script>
        var popup;

        $(document).ready(function () {
            var table = $("#Courses").DataTable({
                ajax: {
                    url: "/api/Courses",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "token"
                        
                    },
                    {
                        data: "name"
                    },
                    {
                        data: "phone",
                        render: function (data) {
                            return data.toString().replace(
                              /(\d\d\d)(\d\d\d)(\d\d\d\d)/g, '$1-$2-$3');
                        }
                    },
                    {
                        data: "address"
                    },
                    {
                        data: "cities.name"
                    },
                    {
                        data: "id",
                        render: function (data, type, Course) {
                            return "<a href='/Courses/details/ " + Course.id + "' class='btn btn-default btn-xs'>" + "<i class='fa fa-folder'></i>View</a>" +
                                //"<button class= 'btn-link js-detail' data-detail-id=" + data + "><i class='fa fa-pencil-square'></i></button>" +
                                "<button class= 'btn btn-danger btn-xs js-delete' data-Course-id=" + data + "><i class='fa fa-trash-o'></i>Delete</button>";
                        }
                    }

                ]
            });


          


            $("#Courses").on("click", ".js-delete", function () {
                var button = $(this);

                bootbox.confirm("Are you sure to delete this Course?", function (result) {
                    if (result) {
                        $.ajax({
                            url: "/api/Courses/" + button.attr("data-Course-id"),
                            method: "DELETE",
                            success: function () {
                                table.row(button.parents("tr")).remove().draw();
                            }
                        });
                    }
                });
            });




        });

    </script>
}



